<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="us.duia.leejo0531.dao.TagMapper">
	<!-- 모든 태그를 가져온다. -->
	<select id="selectTags" resultType="Tag">
		select *
		from Tag
	</select>
	
	<!-- tag가 들어간 질문글을 가져온다. -->
	<select id="getQuestionListByTag" parameterType="Tag" resultType="Tag">
	
	<!-- code here -->
	
	</select>
	
	<!-- tag가 들어간 답변을 가져온다. -->
	<select id="getReplyListByTag" parameterType="Tag" resultType="Tag">
	
	<!-- code here -->
	
	</select>
</mapper>
